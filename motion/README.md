# Cat motion source material

Inputs and generated frames for the cat's head turn and other motion. The
frames the site actually loads are processed from here into `public/cat/motion`.

- `inputs/` — images handed to the image and video models (public URLs are
  needed, so they live in the repo).
